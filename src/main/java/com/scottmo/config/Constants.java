package com.scottmo.config;

import java.util.Locale;

public class Constants {
    public static final String APP_NAME = "Glory Presenter";

    // TODO: make this configurable
    public static final String PRIMARY_LOCALE = Locale.getDefault().toString();
    public static final String SECONDARY_LOCALE = new Locale("zh_CN").toString();
}
